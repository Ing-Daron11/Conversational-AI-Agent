"""
calendar_mcp.py — MCP Tool: Google Calendar

TODO: Implementar en FASE 3
Responsabilidad:
  - Exponer tools MCP para el agente:
      · create_event(title, date, time, description) → crea evento en Google Calendar
      · list_events(date_range) → retorna eventos del período solicitado
      · delete_event(event_id) → cancela un evento

CONCEPTO MCP:
  Model Context Protocol define un contrato estándar para que un LLM
  llame a funciones externas. Cada tool tiene:
    - nombre: identificador que el LLM usa para invocarla
    - descripción: texto que guía al LLM sobre cuándo usarla
    - input_schema: JSON Schema de los parámetros esperados
"""

# Este módulo se implementa en FASE 3
